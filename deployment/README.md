# Deployment Infrastructure

Included deployment assets:

- `/app/Dockerfile` (single-container app image)
- `/app/docker-compose.yml` (app + mongodb)

The FastAPI backend exposes Swagger docs at `/docs` automatically.
